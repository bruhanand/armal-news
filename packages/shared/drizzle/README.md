# Migrations

Generated by `drizzle-kit generate`. Apply with `pnpm db:migrate` (or
`pnpm --filter @armal/shared db:migrate`).

## 0001_tan_captain_midlands

Adds `external_id` and `slug` (`text NOT NULL UNIQUE`) to `stories`. Both
columns are required without a default. The `stories` table is empty in
local dev (the only writer in slice 0001 was `POST /api/dev/seed-one`,
which produced disposable rows). If your local DB has any pre-existing
rows from that route, run `TRUNCATE TABLE stories;` before applying or
the migration will fail with a `column contains null values` error.

## 0002_wet_stryfe

Flips `image_url` from nullable to `NOT NULL`. US 26 mandates every
Story has an image, and slice 0003's ingest pipeline now uploads to
Supabase Storage and writes the resulting CDN URL — no Story should
ever land in the table without one. No backfill default is provided
on purpose; that would mask the invariant. If your local DB has rows
from earlier slices with NULL `image_url`, either backfill them or run
`TRUNCATE TABLE stories;` before applying. Prod has no rows yet.
