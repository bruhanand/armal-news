# Armal News

A TikTok-style AI-news app for niche AI/tech categories. Stories are researched and fully generated by an external personal AI agent (**OpenClaw**), reviewed by hand in the **Admin Dashboard**, and consumed anonymously by readers in the **News App** (web + iOS + Android).

> **Status:** planning. No application code yet — this repo currently contains the domain model (`CONTEXT.md`), architecture decisions (`docs/adr/`), the MVP PRD (`docs/prd/0001-mvp.md`), and the OpenClaw output probe (`ai_news_feed_comprehensive.json`). The tech stack and topology are locked; implementation has not started.

## What's here

```
.
├── CONTEXT.md                         Domain language + locked MVP scope (source of truth)
├── CLAUDE.md                          Project memory for Claude Code (constraints, MCP rules)
├── docs/
│   ├── adr/                           Architecture decision records
│   │   ├── 0001-shared-postgres-…    Admin + News App share one Postgres
│   │   ├── 0002-react-native-expo-…  Mobile = RN/Expo; web = Next.js
│   │   └── 0003-personal-cms-topology Admin Dashboard runs on the laptop, not a server
│   └── prd/
│       └── 0001-mvp.md                MVP product requirements
├── ai_news_feed_comprehensive.json    OpenClaw output probe (not the final schema)
├── PRODUCT_BLUEPRINT.md               (historical) early planning
├── PARTNER_PITCH.md                   (historical) early planning
├── IMPLEMENTATION_GUIDE.md            (historical) early planning
└── IMPLEMENTATION_PLAN.md             (historical) early planning
```

> Where the historical planning docs conflict with `CONTEXT.md` or the ADRs, `CONTEXT.md` and the ADRs win.

## Architecture at a glance

```
┌──────────────────────┐       Tailscale       ┌──────────────────────┐
│  Linux laptop        │  ───── HTTPS ─────▶   │  MacBook             │
│                      │                       │                      │
│  OpenClaw            │   POST batches of     │  Admin Dashboard     │
│  (personal AI agent) │   fully-formed        │  Next.js             │
│                      │   Stories             │  localhost:3001      │
└──────────────────────┘                       └──────────┬───────────┘
                                                          │
                                                          │ Drizzle / SQL
                                                          ▼
                                            ┌──────────────────────────┐
                                            │  Supabase (cloud)        │
                                            │  • Postgres              │
                                            │  • Storage (images CDN)  │
                                            └──────────┬───────────────┘
                                                       │
                              ┌────────────────────────┴────────────────────────┐
                              │                                                 │
                              ▼                                                 ▼
                    ┌──────────────────────┐                      ┌──────────────────────┐
                    │  News App (web)      │                      │  News App (mobile)   │
                    │  Next.js on Vercel   │  ◀── /api/* ──       │  React Native + Expo │
                    │  SSR /story/[slug]   │                      │  iOS + Android       │
                    │  Desktop PWA         │                      │                      │
                    └──────────┬───────────┘                      └──────────┬───────────┘
                               │                                             │
                               │   anonymous reads, status='published' only  │
                               ▼                                             ▼
                    ┌──────────────────────────────────────────────────────────┐
                    │                       Readers                            │
                    └──────────────────────────────────────────────────────────┘
```

Key points (full reasoning lives in [`docs/adr/`](docs/adr/)):

- **Single shared Postgres** — Admin Dashboard and News App share one DB. Publication is a `status` flip on the `stories` row, not an HTTP call. ([ADR-0001](docs/adr/0001-shared-postgres-between-admin-and-news-app.md))
- **Two mobile/web codebases** — RN/Expo for iOS/Android, Next.js for the web. Apple/Play Store policy makes a PWA-only mobile launch a non-starter. ([ADR-0002](docs/adr/0002-react-native-expo-mobile-nextjs-web.md))
- **Admin Dashboard runs on the admin's MacBook** — not deployed in MVP. OpenClaw reaches it over Tailscale; no public webhook, no admin auth provider, no internet-exposed admin URL. Postgres + Storage stay cloud-hosted so readers are unaffected when the laptop sleeps. ([ADR-0003](docs/adr/0003-personal-cms-topology.md))

## Story lifecycle

1. **OpenClaw** generates a batch of fully-formed Stories (title + short summary + body + image + source link + categories + tags + external_id) on the Linux laptop.
2. OpenClaw POSTs the batch to the **Admin Dashboard** at `localhost:3001` over Tailscale. Each Story lands in Postgres as `status='draft'`.
3. The **Admin** reviews each draft, optionally edits text, and clicks **Publish** (`status='published'`) or **Reject** (`status='rejected'`).
4. The **News App** (web + mobile) immediately serves any Story with `status='published'`.

## Tech stack (locked)

| Layer            | Choice                                                                    |
|------------------|---------------------------------------------------------------------------|
| Monorepo         | pnpm workspaces + Turborepo                                               |
| Apps             | `apps/web` (Next.js), `apps/mobile` (Expo), `apps/admin` (Next.js)        |
| Shared           | `packages/shared` — zod schemas, types, DB schema, design tokens          |
| ORM / DB         | Drizzle ORM against Postgres (Supabase); SQL migrations via `drizzle-kit` |
| Web hosting      | Vercel (free hobby tier)                                                  |
| Admin hosting    | Admin's MacBook at `0.0.0.0:3001` (Tailscale-only)                        |
| Image storage    | Supabase Storage + CDN                                                    |
| Article format   | CommonMark Markdown, sanitized at ingest                                  |
| Mobile binaries  | EAS Build → App Store + Play Store                                        |

## MVP scope

Locked in [`CONTEXT.md`](CONTEXT.md). High-level: English only, no audio, anonymous reading, vertical-snap card feed, nine seeded categories with one-at-a-time filtering, free-form tags surfaced on deep-dive only, light/dark theme, SSR'd shareable `/story/[slug]` URLs, native share on mobile, desktop-only PWA install. Out of scope: user accounts, onboarding, Hindi/TTS, premium features, AI Journey Timeline, Editor Agent QA. See [`docs/prd/0001-mvp.md`](docs/prd/0001-mvp.md) for the full PRD.

## Storage setup (one-time)

Story images are stored in a Supabase Storage bucket called `story-images`. Postgres for local dev runs in docker-compose, but Storage is always cloud Supabase (per [ADR-0003](docs/adr/0003-personal-cms-topology.md) — readers must be served when the admin's MacBook sleeps).

Create the bucket once in the Supabase dashboard:

1. Open the Supabase project → **Storage** → **New bucket**.
2. Name: `story-images`. **Public bucket: ON** (readers fetch CDN URLs anonymously).
3. No upload policy is required — the admin app uses the service-role key, which bypasses RLS.
4. Copy the project URL and the **service-role** key from **Settings → API** into your local `.env`:

   ```env
   SUPABASE_URL=https://<your-project>.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=<service-role-key>
   ```

The service-role key is for `apps/admin` only. Do **not** put it in `apps/web`'s environment — the web app only needs `SUPABASE_URL` (used by `next.config` to whitelist the CDN host for `next/image`).

## Constraints

- **Do not use the Supabase MCP server** for this project. The `mcp__claude_ai_Supabase__*` tools are connected to a different project and would target the wrong database. Treat Supabase as vanilla Postgres + Storage; manage migrations by hand via `drizzle-kit` + the `supabase` CLI / `psql`. See [`CLAUDE.md`](CLAUDE.md).
- **No app-level auth on the Admin Dashboard in MVP.** Tailscale is the security boundary.
- **Slugs are immutable** once assigned to a Story. Title edits don't rotate the public URL.
- **Schema migrations apply to web + admin in lock-step** — there's no version-skew between writer and reader.

## Migration trigger

Once revenue justifies the ops cost, the Admin Dashboard and OpenClaw move to cloud hosting and an auth provider gets bolted on. Schema, API contracts, and code paths don't change — only deployment topology. See [ADR-0003](docs/adr/0003-personal-cms-topology.md).

## Brand

- **Name:** Armal News
- **Reference aesthetic:** Anthropic / Claude — warm-paper background, editorial-serif typography (Newsreader), sparing coral accent, restrained chrome
- **Type:** Newsreader (headings + italic summaries), Inter (body + UI), JetBrains Mono (code)
- **Color tokens:** see `CONTEXT.md` § "Design language"
